# Workflow catalog

These workflows are realistic maintained examples. They may require local tools or project connection configuration. Use `work-item-to-pr` for Plane, Jira, or GitHub Issues work items; provider selection belongs in `.tesseraft/project.json`.
