# Tutorials

Start with [`smoke`](smoke/workflow.edn), then run [`mock-run-workflow`](mock-run-workflow/workflow.edn) to exercise an agent node without live credentials. The maintained catalog provides larger compositions after these basics.
