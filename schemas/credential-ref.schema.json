{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Tesseraft Credential Reference",
  "description": "A credential reference of the form `<store>:<path>`. Raw credentials are kept out of repositories; only the reference is persisted in project manifests. Initially only `env:` is wired for local single-user resolution; `github-actions:` is validated-but-unresolved locally.",
  "type": "string",
  "pattern": "^(env|github-actions):\\S+$"
}