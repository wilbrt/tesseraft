{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Tesseraft User-local Credential Store",
  "description": "User-owned local credential store under $TESSERAFT_HOME. It stores credential values outside repository-owned descriptors; repository state should persist credential references only.",
  "type": "object",
  "required": ["version", "credentials"],
  "properties": {
    "version": { "const": 1, "description": "Local credential store schema version." },
    "credentials": {
      "type": "object",
      "description": "Named credential values addressable by tesseraft: references.",
      "additionalProperties": { "type": "string", "minLength": 1 }
    }
  },
  "additionalProperties": false
}
