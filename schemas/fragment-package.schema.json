{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Tesseraft Self-contained Fragment Package",
  "description": "A reusable multi-node subgraph with a declared boundary contract (tesseraft.fragment/v1). Inclusion lints the boundary; internal proof is done once by lint-fragment-package.",
  "type": "object",
  "required": [
    "api-version",
    "kind",
    "metadata",
    "interface",
    "fragment"
  ],
  "$defs": {
    "resourceDeclaration": {
      "type": "object",
      "properties": {
        "requires": { "$ref": "#/$defs/resourceList" },
        "consumes": { "$ref": "#/$defs/resourceList" },
        "produces": { "$ref": "#/$defs/resourceList" }
      },
      "additionalProperties": true
    },
    "resourceList": {
      "type": "array",
      "items": { "$ref": "#/$defs/resource" }
    },
    "resource": {
      "type": "object",
      "required": ["kind", "name"],
      "properties": {
        "kind": { "type": "string" },
        "name": { "type": "string" },
        "path": { "type": "string" },
        "mode": { "type": "string" },
        "description": { "type": "string" },
        "schema": { "type": "string" },
        "source": { "type": "string" },
        "tool": { "type": "string" },
        "secret": { "type": ["string", "boolean"] },
        "handler": { "type": "string" },
        "executor": { "type": "string" }
      },
      "additionalProperties": true
    },
    "transition": {
      "type": "object",
      "required": ["next"],
      "properties": {
        "next": { "type": "string", "minLength": 1 },
        "effects": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "when": { "type": "object" }
      },
      "additionalProperties": true
    },
    "fragmentState": {
      "type": "object",
      "required": ["type"],
      "allOf": [
        {
          "if": { "properties": { "type": { "const": "terminal" } }, "required": ["type"] },
          "then": {
            "required": ["outcome"],
            "properties": {
              "outcome": { "type": "string", "minLength": 1 }
            }
          }
        },
        {
          "not": {
            "properties": { "type": { "const": "fragment" } },
            "required": ["type"]
          }
        }
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["agent", "deterministic", "process", "timer", "approval", "router", "terminal"]
        },
        "handler": { "type": "string", "minLength": 1 },
        "executor": { "type": "string", "minLength": 1 },
        "tools": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "next": { "type": "string", "minLength": 1 },
        "transitions": {
          "type": "array",
          "items": { "$ref": "#/$defs/transition" }
        }
      },
      "additionalProperties": true
    }
  },
  "properties": {
    "api-version": { "const": "tesseraft.fragment/v1" },
    "kind": { "const": "fragment" },
    "metadata": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "version": { "type": "string" },
        "authors": { "type": "array", "items": { "type": "string" } },
        "tags": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": true
    },
    "interface": {
      "type": "object",
      "required": ["outcomes"],
      "properties": {
        "inputs": { "type": "object" },
        "parameters": { "type": "object" },
        "outputs": { "type": "object" },
        "outcomes": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "minItems": 1
        }
      },
      "additionalProperties": true
    },
    "requirements": {
      "type": "object",
      "properties": {
        "executors": { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
        "handlers": { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
        "tools": { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
        "secrets": { "type": "array", "items": { "type": "string" } },
        "template-vars": { "type": "array", "items": { "type": "string" } },
        "resources": { "$ref": "#/$defs/resourceDeclaration" }
      },
      "additionalProperties": true
    },
    "assets": {
      "type": "object",
      "properties": {
        "prompts": { "type": "array", "items": { "type": "string" } },
        "scripts": { "type": "array", "items": { "type": "string" } },
        "schemas": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": true
    },
    "fragment": {
      "type": "object",
      "required": ["initial", "exit", "states"],
      "properties": {
        "initial": { "type": "string" },
        "defaults": { "type": "object" },
        "policies": { "type": "object" },
        "entry": {
          "type": "object",
          "properties": {
            "inputs": { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
            "parameters": { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true }
          },
          "additionalProperties": true
        },
        "exit": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["on"],
            "properties": {
              "on": { "type": "string", "minLength": 1 },
              "produces": { "type": "object" }
            },
            "additionalProperties": true
          }
        },
        "states": {
          "type": "object",
          "patternProperties": {
            ".*": { "$ref": "#/$defs/fragmentState" }
          },
          "additionalProperties": false
        },
        "resources": { "$ref": "#/$defs/resourceDeclaration" }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}