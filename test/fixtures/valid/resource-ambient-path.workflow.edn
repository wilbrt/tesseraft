{:api-version "tesseraft.workflow/v1"
 :kind :workflow
 :metadata {:name "resource-ambient-path"}
 :inputs {:prompt {:path "prompt/prompt.md" :required true
                   :resource-name "user-prompt"}}
 :defaults {:config {:path "config/defaults.edn"
                     :name "default-config"}
            :max-rounds 1
            :state-timeout "1m"}
 :policies {:require-timeouts true :require-max-rounds true}
 :initial :consumer
 :states {:consumer {:type :deterministic
                     :handler :noop/succeed
                     :runtime {:timeout "1m"}
                     :resources {:requires [{:kind :input
                                             :name "user-prompt"
                                             :path "prompt/prompt.md"
                                             :mode :read}
                                            {:kind :input
                                             :name "prompt"
                                             :path "prompt/prompt.md"
                                             :mode :read}
                                            {:kind :default
                                             :name "default-config"
                                             :path "config/defaults.edn"
                                             :mode :read}]}
                     :next :done}
          :done {:type :terminal :status :success}}}
