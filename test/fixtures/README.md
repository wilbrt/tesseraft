# Regression fixtures

Fixtures exist to prove schema, linter, migration, and runtime behavior. `valid/` contains accepted boundary cases; `invalid/` contains deliberate failures. They are not public tutorials and may be minimal, awkward, or intentionally malformed.
